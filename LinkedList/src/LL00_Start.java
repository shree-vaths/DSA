
public class LL00_Start {
	/*
	 * Array is predefined contiguos memory locaton.
	 * LL is not predefined or contiguos, and can be increased or decreased
	 * constant accessing using index cant be done
	 * Consider 0 based index
	 * With tail, insertion at the end becomes O(1)
	 * 
	 * 
	 * Missing -
	 * L7 - Sort
	 */
}
