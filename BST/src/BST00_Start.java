
public class BST00_Start {
	/*
	 * The left child is equal to or less than root.
	 * Try to avoid equal to in left node. Avoid duplicates
	 * The right child is always greater then the root.
	 * The insertion, searching and deletion always takes half of normal tree
	 * 
	 * The inorder of BST produces the ascending order of nos
	 * 
	 * The root is larger than largest element in left sub tree
	 * The root is smaller than smallest element in right sub tree
	 * 
	 * Missing -
	 * L51 - Two Sum BST
	 */
}
